# HEY-CAPSULE-PACK PATCH 0011 (REMOVABLE): cross-platform pipe for native Windows/MSVC.
# KILL CONDITION: drop when upstream supports Windows/MSVC natively.
--- a/elastos/crates/elastos-compute/src/providers/wasm.rs
+++ b/elastos/crates/elastos-compute/src/providers/wasm.rs
@@ -261,18 +261,28 @@
     }
 
     /// Create an OS pipe pair, returning (read_end, write_end) as `std::fs::File`.
+    /// Cross-platform (was Unix-only libc::pipe + FromRawFd): Rust 1.89's
+    /// std::io::pipe() + per-platform OwnedFd/OwnedHandle -> File. The WASM capsule
+    /// stdio bridge is core to lite, so this must also work on native Windows.
     fn create_pipe() -> Result<(std::fs::File, std::fs::File)> {
-        let mut fds = [0i32; 2];
-        let ret = unsafe { libc::pipe(fds.as_mut_ptr()) };
-        if ret != 0 {
-            return Err(ElastosError::Compute(format!(
-                "Failed to create pipe: {}",
-                std::io::Error::last_os_error()
-            )));
+        let (reader, writer) = std::io::pipe()
+            .map_err(|e| ElastosError::Compute(format!("Failed to create pipe: {}", e)))?;
+        #[cfg(unix)]
+        {
+            use std::os::fd::OwnedFd;
+            Ok((
+                std::fs::File::from(OwnedFd::from(reader)),
+                std::fs::File::from(OwnedFd::from(writer)),
+            ))
+        }
+        #[cfg(windows)]
+        {
+            use std::os::windows::io::OwnedHandle;
+            Ok((
+                std::fs::File::from(OwnedHandle::from(reader)),
+                std::fs::File::from(OwnedHandle::from(writer)),
+            ))
         }
-        let read_end = unsafe { std::os::unix::io::FromRawFd::from_raw_fd(fds[0]) };
-        let write_end = unsafe { std::os::unix::io::FromRawFd::from_raw_fd(fds[1]) };
-        Ok((read_end, write_end))
     }
 
     /// Execute a WASM module with WASI
--- a/elastos/crates/elastos-crosvm/Cargo.toml
+++ b/elastos/crates/elastos-crosvm/Cargo.toml
@@ -12,7 +12,7 @@
 tokio = { version = "1.0", features = ["full", "process"] }
 serde = { version = "1.0", features = ["derive"] }
 serde_json = "1.0"
-nix = { version = "0.29", features = ["process", "signal"] }
+# nix gated to unix (unix-only crate) — see [target.cfg(unix).dependencies]
 uuid = { version = "1.0", features = ["v4"] }
 async-trait = "0.1"
 tracing = "0.1"
@@ -22,3 +22,8 @@
 [dev-dependencies]
 tokio = { version = "1.0", features = ["full", "test-util"] }
 tempfile = "3.10"
+
+# nix is a Unix-only crate (won't compile on windows-msvc). The microVM lifecycle
+# (vm.rs signal::kill) that uses it is dead-in-lite; gate the dep + guard its uses.
+[target.'cfg(unix)'.dependencies]
+nix = { version = "0.29", features = ["process", "signal"] }
--- a/elastos/crates/elastos-crosvm/src/lib.rs
+++ b/elastos/crates/elastos-crosvm/src/lib.rs
@@ -36,6 +36,13 @@
 
 /// Check if the system supports crosvm (has KVM).
 /// If this returns false, capsule launch will fail hard.
+#[cfg(unix)]
 pub fn is_supported() -> bool {
     std::path::Path::new("/dev/kvm").exists()
 }
+
+/// crosvm/KVM is Linux-only; never supported off-unix (e.g. native Windows).
+#[cfg(not(unix))]
+pub fn is_supported() -> bool {
+    false
+}
--- a/elastos/crates/elastos-crosvm/src/config.rs
+++ b/elastos/crates/elastos-crosvm/src/config.rs
@@ -83,13 +83,21 @@
 fn default_data_dir() -> PathBuf {
     // Prefer $HOME, fall back to passwd entry (survives setcap/AT_SECURE)
     let home = std::env::var_os("HOME").map(PathBuf::from).or_else(|| {
-        let uid = unsafe { libc::getuid() };
-        let pw = unsafe { libc::getpwuid(uid) };
-        if !pw.is_null() {
-            let dir = unsafe { std::ffi::CStr::from_ptr((*pw).pw_dir) };
-            dir.to_str().ok().map(PathBuf::from)
-        } else {
-            None
+        #[cfg(unix)]
+        {
+            // passwd fallback survives setcap/AT_SECURE (Unix only).
+            let uid = unsafe { libc::getuid() };
+            let pw = unsafe { libc::getpwuid(uid) };
+            if pw.is_null() {
+                None
+            } else {
+                let dir = unsafe { std::ffi::CStr::from_ptr((*pw).pw_dir) };
+                dir.to_str().ok().map(PathBuf::from)
+            }
+        }
+        #[cfg(not(unix))]
+        {
+            std::env::var_os("USERPROFILE").map(PathBuf::from)
         }
     });
     home.map(|h| h.join(".local/share/elastos"))
--- a/elastos/crates/elastos-crosvm/src/network.rs
+++ b/elastos/crates/elastos-crosvm/src/network.rs
@@ -4,96 +4,17 @@
 //! Requires CAP_NET_ADMIN on the runtime binary.
 //! Ordinary app capsules should stay on the serial Carrier bridge and not use this.
 //! No iptables, no ip_forward — guest-network capsules can only reach the host runtime.
+//!
+//! The TAP machinery is Linux-only. On non-unix targets (e.g. the native Windows
+//! desktop build) microVMs are unsupported (`crate::is_supported()` is false), so
+//! `NetworkConfig` keeps its data shape but `setup`/`teardown` are no-ops and the
+//! ioctl/FFI internals are compiled out.
 
-use std::os::fd::{AsRawFd, FromRawFd, OwnedFd};
 use std::sync::atomic::AtomicI32;
 
-use elastos_common::{ElastosError, Result};
+use elastos_common::Result;
 
-// ── Linux ioctl constants ──────────────────────────────────────────
-
-// TUNSETIFF = _IOW('T', 202, int) = 0x400454ca (same on x86_64 and aarch64)
-// ioctl uses nix::ioctl_write_ptr_bad! pattern, but we avoid the nix dep.
-// libc::ioctl second param is c_ulong on x86_64, c_int on aarch64.
-// We use the nix approach: define as u32, cast at call site.
-const TUNSETIFF: u32 = 0x400454ca;
-const TUNSETOWNER: u32 = 0x400454cc;
-const IFF_TAP: libc::c_short = 0x0002;
-const IFF_NO_PI: libc::c_short = 0x1000;
-const SIOCGIFFLAGS: u32 = 0x8913;
-const SIOCSIFFLAGS: u32 = 0x8914;
-const SIOCSIFADDR: u32 = 0x8916;
-const SIOCSIFNETMASK: u32 = 0x891c;
-const IFF_UP: libc::c_short = 0x1;
-
-/// Call ioctl with the correct type for the platform.
-/// libc::ioctl takes c_ulong on glibc x86_64, c_int everywhere else (musl, aarch64).
-unsafe fn ioctl_raw(fd: i32, request: u32, arg: *mut libc::c_void) -> i32 {
-    // Cast to the platform's ioctl request type at the call site.
-    // On all Linux targets, the kernel ioctl number fits in 32 bits.
-    #[cfg(target_env = "gnu")]
-    {
-        libc::ioctl(fd, request as libc::c_ulong, arg)
-    }
-    #[cfg(not(target_env = "gnu"))]
-    {
-        libc::ioctl(fd, request as libc::c_int, arg)
-    }
-}
-
-// ── FFI struct ─────────────────────────────────────────────────────
-
-/// Mirrors `struct ifreq` from <net/if.h>.
-#[repr(C)]
-struct Ifreq {
-    ifr_name: [u8; libc::IFNAMSIZ], // 16 bytes
-    ifr_data: [u8; 24],             // union: sockaddr, flags, etc.
-}
-
-impl Ifreq {
-    fn new(name: &str) -> Self {
-        let mut ifr: Self = unsafe { std::mem::zeroed() };
-        let bytes = name.as_bytes();
-        let len = bytes.len().min(libc::IFNAMSIZ - 1);
-        ifr.ifr_name[..len].copy_from_slice(&bytes[..len]);
-        ifr
-    }
-
-    fn set_addr(&mut self, ip: std::net::Ipv4Addr) {
-        let sa = libc::sockaddr_in {
-            sin_family: libc::AF_INET as libc::sa_family_t,
-            sin_port: 0,
-            sin_addr: libc::in_addr {
-                s_addr: u32::from(ip).to_be(),
-            },
-            sin_zero: [0; 8],
-        };
-        let bytes: [u8; std::mem::size_of::<libc::sockaddr_in>()] =
-            unsafe { std::mem::transmute(sa) };
-        self.ifr_data[..bytes.len()].copy_from_slice(&bytes);
-    }
-
-    fn set_flags(&mut self, flags: libc::c_short) {
-        self.ifr_data[..2].copy_from_slice(&flags.to_ne_bytes());
-    }
-
-    fn get_flags(&self) -> libc::c_short {
-        libc::c_short::from_ne_bytes([self.ifr_data[0], self.ifr_data[1]])
-    }
-}
-
-fn udp_socket() -> Result<OwnedFd> {
-    let fd = unsafe { libc::socket(libc::AF_INET, libc::SOCK_DGRAM | libc::SOCK_CLOEXEC, 0) };
-    if fd < 0 {
-        return Err(ElastosError::Compute(format!(
-            "socket() failed: {}",
-            std::io::Error::last_os_error()
-        )));
-    }
-    Ok(unsafe { OwnedFd::from_raw_fd(fd) })
-}
-
-// ── NetworkConfig ──────────────────────────────────────────────────
+// ── NetworkConfig (cross-platform definition) ──────────────────────
 
 /// Network configuration for a VM.
 /// Each VM gets a private /30 point-to-point link to the host.
@@ -156,183 +77,295 @@
             _tap_fd: AtomicI32::new(-1),
         }
     }
+}
 
-    /// Create the TAP device and configure the network.
-    /// Requires CAP_NET_ADMIN (set via `setcap cap_net_admin+ep` on the binary).
+// ── Non-unix stub ──────────────────────────────────────────────────
+// microVMs are unsupported off-Linux; keep the API so crosvm compiles.
+
+#[cfg(not(unix))]
+impl NetworkConfig {
+    /// No-op: guest-network TAP devices are Linux-only.
     pub fn setup(&self) -> Result<()> {
-        // 1. Create TAP device
-        let fd = self.create_tap()?;
+        Ok(())
+    }
 
-        // 2. Assign host IP
-        self.set_ip()?;
+    /// No-op: guest-network TAP devices are Linux-only.
+    pub fn teardown(&self) -> Result<()> {
+        Ok(())
+    }
+}
 
-        // 3. Set netmask
-        self.set_netmask()?;
+// ── Unix TAP implementation (ioctl/FFI) ────────────────────────────
 
-        // 4. Bring interface up
-        self.bring_up()?;
-
-        // 5. Make TAP persistent and close our fd.
-        // TUNSETPERSIST keeps the device alive after we close the fd.
-        // crosvm will reopen it by name via --net tap-name=...
-        const TUNSETPERSIST: u32 = 0x400454cb;
-        unsafe {
-            ioctl_raw(fd, TUNSETPERSIST, std::ptr::without_provenance_mut(1));
-        }
-        unsafe {
-            libc::close(fd);
-        }
-
-        tracing::info!(
-            "Guest-network TAP configured (ioctl): tap={} host={} guest={}",
-            self.tap_name,
-            self.host_ip,
-            self.guest_ip
-        );
+#[cfg(unix)]
+mod tap {
+    use std::os::fd::{AsRawFd, FromRawFd, OwnedFd};
+
+    use elastos_common::{ElastosError, Result};
+
+    use super::NetworkConfig;
+
+    // ── Linux ioctl constants ──────────────────────────────────────
+
+    // TUNSETIFF = _IOW('T', 202, int) = 0x400454ca (same on x86_64 and aarch64)
+    // ioctl uses nix::ioctl_write_ptr_bad! pattern, but we avoid the nix dep.
+    // libc::ioctl second param is c_ulong on x86_64, c_int on aarch64.
+    // We use the nix approach: define as u32, cast at call site.
+    const TUNSETIFF: u32 = 0x400454ca;
+    const TUNSETOWNER: u32 = 0x400454cc;
+    const IFF_TAP: libc::c_short = 0x0002;
+    const IFF_NO_PI: libc::c_short = 0x1000;
+    const SIOCGIFFLAGS: u32 = 0x8913;
+    const SIOCSIFFLAGS: u32 = 0x8914;
+    const SIOCSIFADDR: u32 = 0x8916;
+    const SIOCSIFNETMASK: u32 = 0x891c;
+    const IFF_UP: libc::c_short = 0x1;
+
+    /// Call ioctl with the correct type for the platform.
+    /// libc::ioctl takes c_ulong on glibc x86_64, c_int everywhere else (musl, aarch64).
+    unsafe fn ioctl_raw(fd: i32, request: u32, arg: *mut libc::c_void) -> i32 {
+        // Cast to the platform's ioctl request type at the call site.
+        // On all Linux targets, the kernel ioctl number fits in 32 bits.
+        #[cfg(target_env = "gnu")]
+        {
+            libc::ioctl(fd, request as libc::c_ulong, arg)
+        }
+        #[cfg(not(target_env = "gnu"))]
+        {
+            libc::ioctl(fd, request as libc::c_int, arg)
+        }
+    }
 
-        Ok(())
+    // ── FFI struct ─────────────────────────────────────────────────
+
+    /// Mirrors `struct ifreq` from <net/if.h>.
+    #[repr(C)]
+    struct Ifreq {
+        ifr_name: [u8; libc::IFNAMSIZ], // 16 bytes
+        ifr_data: [u8; 24],             // union: sockaddr, flags, etc.
     }
 
-    /// Tear down the TAP device.
-    pub fn teardown(&self) -> Result<()> {
-        // Open the persistent TAP to clear TUNSETPERSIST, then close.
-        // This destroys the device.
-        let fd = unsafe { libc::open(c"/dev/net/tun".as_ptr(), libc::O_RDWR) };
-        if fd >= 0 {
-            let mut ifr = Ifreq::new(&self.tap_name);
-            ifr.set_flags(IFF_TAP | IFF_NO_PI);
-            if unsafe { ioctl_raw(fd, TUNSETIFF, &mut ifr as *mut Ifreq as *mut _) } == 0 {
-                const TUNSETPERSIST: u32 = 0x400454cb;
-                unsafe {
-                    ioctl_raw(fd, TUNSETPERSIST, std::ptr::null_mut());
-                }
-            }
-            unsafe {
-                libc::close(fd);
-            }
-            tracing::info!("Guest-network TAP torn down: tap={}", self.tap_name);
+    impl Ifreq {
+        fn new(name: &str) -> Self {
+            let mut ifr: Self = unsafe { std::mem::zeroed() };
+            let bytes = name.as_bytes();
+            let len = bytes.len().min(libc::IFNAMSIZ - 1);
+            ifr.ifr_name[..len].copy_from_slice(&bytes[..len]);
+            ifr
+        }
+
+        fn set_addr(&mut self, ip: std::net::Ipv4Addr) {
+            let sa = libc::sockaddr_in {
+                sin_family: libc::AF_INET as libc::sa_family_t,
+                sin_port: 0,
+                sin_addr: libc::in_addr {
+                    s_addr: u32::from(ip).to_be(),
+                },
+                sin_zero: [0; 8],
+            };
+            let bytes: [u8; std::mem::size_of::<libc::sockaddr_in>()] =
+                unsafe { std::mem::transmute(sa) };
+            self.ifr_data[..bytes.len()].copy_from_slice(&bytes);
+        }
+
+        fn set_flags(&mut self, flags: libc::c_short) {
+            self.ifr_data[..2].copy_from_slice(&flags.to_ne_bytes());
+        }
+
+        fn get_flags(&self) -> libc::c_short {
+            libc::c_short::from_ne_bytes([self.ifr_data[0], self.ifr_data[1]])
         }
-        Ok(())
     }
 
-    fn create_tap(&self) -> Result<i32> {
-        let fd = unsafe { libc::open(c"/dev/net/tun".as_ptr(), libc::O_RDWR | libc::O_CLOEXEC) };
+    fn udp_socket() -> Result<OwnedFd> {
+        let fd = unsafe { libc::socket(libc::AF_INET, libc::SOCK_DGRAM | libc::SOCK_CLOEXEC, 0) };
         if fd < 0 {
             return Err(ElastosError::Compute(format!(
-                "open(/dev/net/tun) failed: {}. Run: sudo setcap cap_net_admin+ep $(which elastos)",
+                "socket() failed: {}",
                 std::io::Error::last_os_error()
             )));
         }
+        Ok(unsafe { OwnedFd::from_raw_fd(fd) })
+    }
+
+    impl NetworkConfig {
+        /// Create the TAP device and configure the network.
+        /// Requires CAP_NET_ADMIN (set via `setcap cap_net_admin+ep` on the binary).
+        pub fn setup(&self) -> Result<()> {
+            // 1. Create TAP device
+            let fd = self.create_tap()?;
+
+            // 2. Assign host IP
+            self.set_ip()?;
+
+            // 3. Set netmask
+            self.set_netmask()?;
 
-        let mut ifr = Ifreq::new(&self.tap_name);
-        ifr.set_flags(IFF_TAP | IFF_NO_PI);
+            // 4. Bring interface up
+            self.bring_up()?;
 
-        if unsafe { ioctl_raw(fd, TUNSETIFF, &mut ifr as *mut Ifreq as *mut _) } < 0 {
-            let err = std::io::Error::last_os_error();
+            // 5. Make TAP persistent and close our fd.
+            // TUNSETPERSIST keeps the device alive after we close the fd.
+            // crosvm will reopen it by name via --net tap-name=...
+            const TUNSETPERSIST: u32 = 0x400454cb;
+            unsafe {
+                ioctl_raw(fd, TUNSETPERSIST, std::ptr::without_provenance_mut(1));
+            }
             unsafe {
                 libc::close(fd);
             }
-            return Err(ElastosError::Compute(format!(
-                "TUNSETIFF failed for '{}': {}. Run: sudo setcap cap_net_admin+ep $(which elastos)",
-                self.tap_name, err
-            )));
-        }
 
-        // Set owner to current user
-        let uid = unsafe { libc::geteuid() } as libc::c_ulong;
-        if unsafe { ioctl_raw(fd, TUNSETOWNER, uid as *mut _) } < 0 {
-            tracing::warn!(
-                "TUNSETOWNER failed (non-fatal): {}",
-                std::io::Error::last_os_error()
+            tracing::info!(
+                "Guest-network TAP configured (ioctl): tap={} host={} guest={}",
+                self.tap_name,
+                self.host_ip,
+                self.guest_ip
             );
+
+            Ok(())
         }
 
-        Ok(fd)
-    }
+        /// Tear down the TAP device.
+        pub fn teardown(&self) -> Result<()> {
+            // Open the persistent TAP to clear TUNSETPERSIST, then close.
+            // This destroys the device.
+            let fd = unsafe { libc::open(c"/dev/net/tun".as_ptr(), libc::O_RDWR) };
+            if fd >= 0 {
+                let mut ifr = Ifreq::new(&self.tap_name);
+                ifr.set_flags(IFF_TAP | IFF_NO_PI);
+                if unsafe { ioctl_raw(fd, TUNSETIFF, &mut ifr as *mut Ifreq as *mut _) } == 0 {
+                    const TUNSETPERSIST: u32 = 0x400454cb;
+                    unsafe {
+                        ioctl_raw(fd, TUNSETPERSIST, std::ptr::null_mut());
+                    }
+                }
+                unsafe {
+                    libc::close(fd);
+                }
+                tracing::info!("Guest-network TAP torn down: tap={}", self.tap_name);
+            }
+            Ok(())
+        }
 
-    fn set_ip(&self) -> Result<()> {
-        let sock = udp_socket()?;
-        let ip: std::net::Ipv4Addr = self
-            .host_ip
-            .parse()
-            .map_err(|e| ElastosError::Compute(format!("Invalid IP '{}': {}", self.host_ip, e)))?;
-        let mut ifr = Ifreq::new(&self.tap_name);
-        ifr.set_addr(ip);
-        if unsafe {
-            ioctl_raw(
-                sock.as_raw_fd(),
-                SIOCSIFADDR,
-                &ifr as *const Ifreq as *mut _,
-            )
-        } < 0
-        {
-            return Err(ElastosError::Compute(format!(
-                "SIOCSIFADDR failed: {}",
-                std::io::Error::last_os_error()
-            )));
+        fn create_tap(&self) -> Result<i32> {
+            let fd = unsafe { libc::open(c"/dev/net/tun".as_ptr(), libc::O_RDWR | libc::O_CLOEXEC) };
+            if fd < 0 {
+                return Err(ElastosError::Compute(format!(
+                    "open(/dev/net/tun) failed: {}. Run: sudo setcap cap_net_admin+ep $(which elastos)",
+                    std::io::Error::last_os_error()
+                )));
+            }
+
+            let mut ifr = Ifreq::new(&self.tap_name);
+            ifr.set_flags(IFF_TAP | IFF_NO_PI);
+
+            if unsafe { ioctl_raw(fd, TUNSETIFF, &mut ifr as *mut Ifreq as *mut _) } < 0 {
+                let err = std::io::Error::last_os_error();
+                unsafe {
+                    libc::close(fd);
+                }
+                return Err(ElastosError::Compute(format!(
+                    "TUNSETIFF failed for '{}': {}. Run: sudo setcap cap_net_admin+ep $(which elastos)",
+                    self.tap_name, err
+                )));
+            }
+
+            // Set owner to current user
+            let uid = unsafe { libc::geteuid() } as libc::c_ulong;
+            if unsafe { ioctl_raw(fd, TUNSETOWNER, uid as *mut _) } < 0 {
+                tracing::warn!(
+                    "TUNSETOWNER failed (non-fatal): {}",
+                    std::io::Error::last_os_error()
+                );
+            }
+
+            Ok(fd)
         }
-        Ok(())
-    }
 
-    fn set_netmask(&self) -> Result<()> {
-        let sock = udp_socket()?;
-        let mask: std::net::Ipv4Addr = self
-            .mask
-            .parse()
-            .map_err(|e| ElastosError::Compute(format!("Invalid mask '{}': {}", self.mask, e)))?;
-        let mut ifr = Ifreq::new(&self.tap_name);
-        ifr.set_addr(mask);
-        if unsafe {
-            ioctl_raw(
-                sock.as_raw_fd(),
-                SIOCSIFNETMASK,
-                &ifr as *const Ifreq as *mut _,
-            )
-        } < 0
-        {
-            return Err(ElastosError::Compute(format!(
-                "SIOCSIFNETMASK failed: {}",
-                std::io::Error::last_os_error()
-            )));
+        fn set_ip(&self) -> Result<()> {
+            let sock = udp_socket()?;
+            let ip: std::net::Ipv4Addr = self
+                .host_ip
+                .parse()
+                .map_err(|e| ElastosError::Compute(format!("Invalid IP '{}': {}", self.host_ip, e)))?;
+            let mut ifr = Ifreq::new(&self.tap_name);
+            ifr.set_addr(ip);
+            if unsafe {
+                ioctl_raw(
+                    sock.as_raw_fd(),
+                    SIOCSIFADDR,
+                    &ifr as *const Ifreq as *mut _,
+                )
+            } < 0
+            {
+                return Err(ElastosError::Compute(format!(
+                    "SIOCSIFADDR failed: {}",
+                    std::io::Error::last_os_error()
+                )));
+            }
+            Ok(())
         }
-        Ok(())
-    }
 
-    fn bring_up(&self) -> Result<()> {
-        let sock = udp_socket()?;
-        let mut ifr = Ifreq::new(&self.tap_name);
-        if unsafe {
-            ioctl_raw(
-                sock.as_raw_fd(),
-                SIOCGIFFLAGS,
-                &mut ifr as *mut Ifreq as *mut _,
-            )
-        } < 0
-        {
-            return Err(ElastosError::Compute(format!(
-                "SIOCGIFFLAGS failed: {}",
-                std::io::Error::last_os_error()
-            )));
+        fn set_netmask(&self) -> Result<()> {
+            let sock = udp_socket()?;
+            let mask: std::net::Ipv4Addr = self
+                .mask
+                .parse()
+                .map_err(|e| ElastosError::Compute(format!("Invalid mask '{}': {}", self.mask, e)))?;
+            let mut ifr = Ifreq::new(&self.tap_name);
+            ifr.set_addr(mask);
+            if unsafe {
+                ioctl_raw(
+                    sock.as_raw_fd(),
+                    SIOCSIFNETMASK,
+                    &ifr as *const Ifreq as *mut _,
+                )
+            } < 0
+            {
+                return Err(ElastosError::Compute(format!(
+                    "SIOCSIFNETMASK failed: {}",
+                    std::io::Error::last_os_error()
+                )));
+            }
+            Ok(())
         }
-        ifr.set_flags(ifr.get_flags() | IFF_UP);
-        if unsafe {
-            ioctl_raw(
-                sock.as_raw_fd(),
-                SIOCSIFFLAGS,
-                &ifr as *const Ifreq as *mut _,
-            )
-        } < 0
-        {
-            return Err(ElastosError::Compute(format!(
-                "SIOCSIFFLAGS failed: {}",
-                std::io::Error::last_os_error()
-            )));
+
+        fn bring_up(&self) -> Result<()> {
+            let sock = udp_socket()?;
+            let mut ifr = Ifreq::new(&self.tap_name);
+            if unsafe {
+                ioctl_raw(
+                    sock.as_raw_fd(),
+                    SIOCGIFFLAGS,
+                    &mut ifr as *mut Ifreq as *mut _,
+                )
+            } < 0
+            {
+                return Err(ElastosError::Compute(format!(
+                    "SIOCGIFFLAGS failed: {}",
+                    std::io::Error::last_os_error()
+                )));
+            }
+            ifr.set_flags(ifr.get_flags() | IFF_UP);
+            if unsafe {
+                ioctl_raw(
+                    sock.as_raw_fd(),
+                    SIOCSIFFLAGS,
+                    &ifr as *const Ifreq as *mut _,
+                )
+            } < 0
+            {
+                return Err(ElastosError::Compute(format!(
+                    "SIOCSIFFLAGS failed: {}",
+                    std::io::Error::last_os_error()
+                )));
+            }
+            Ok(())
         }
-        Ok(())
     }
 }
 
-// ── Helpers ────────────────────────────────────────────────────────
+// ── Helpers (cross-platform) ───────────────────────────────────────
 
 /// Generate a deterministic MAC address from a VM ID.
 pub fn generate_mac(vm_id: &str) -> String {
--- a/elastos/crates/elastos-crosvm/src/vm.rs
+++ b/elastos/crates/elastos-crosvm/src/vm.rs
@@ -4,12 +4,51 @@
 use std::process::Stdio;
 
 use elastos_common::{CapsuleManifest, CapsuleStatus, ElastosError, Result};
+#[cfg(unix)]
 use nix::sys::signal::{self, Signal};
+#[cfg(unix)]
 use nix::unistd::Pid;
 use tokio::process::{Child, Command};
 
 use crate::config::VmConfig;
 
+// ── Signal helpers (unix-only; microVMs are Linux-only) ─────────────
+// crosvm VMs only run where `crate::is_supported()` is true (Linux + KVM), so on
+// non-unix targets (e.g. the native Windows desktop build) these are unreachable
+// no-ops that keep `RunningVm` compiling without the `nix` dependency.
+
+#[cfg(unix)]
+fn term_pid(pid: u32) {
+    let nix_pid = Pid::from_raw(pid as i32);
+    if let Err(e) = signal::kill(nix_pid, Signal::SIGTERM) {
+        tracing::warn!("Failed to send SIGTERM to VM: {}", e);
+    }
+}
+
+#[cfg(not(unix))]
+fn term_pid(_pid: u32) {}
+
+#[cfg(unix)]
+fn kill_pid(pid: u32) {
+    let nix_pid = Pid::from_raw(pid as i32);
+    if let Err(e) = signal::kill(nix_pid, Signal::SIGKILL) {
+        tracing::warn!("Failed to send SIGKILL to VM: {}", e);
+    }
+}
+
+#[cfg(not(unix))]
+fn kill_pid(_pid: u32) {}
+
+#[cfg(unix)]
+fn pid_alive(pid: u32) -> bool {
+    signal::kill(Pid::from_raw(pid as i32), None).is_ok()
+}
+
+#[cfg(not(unix))]
+fn pid_alive(_pid: u32) -> bool {
+    false
+}
+
 /// A running crosvm VM instance
 pub struct RunningVm {
     /// VM configuration
@@ -206,18 +245,12 @@
                 _ => {
                     // Fallback to SIGTERM
                     if let Some(pid) = self.pid {
-                        let nix_pid = Pid::from_raw(pid as i32);
-                        if let Err(e) = signal::kill(nix_pid, Signal::SIGTERM) {
-                            tracing::warn!("Failed to send SIGTERM to VM: {}", e);
-                        }
+                        term_pid(pid);
                     }
                 }
             }
         } else if let Some(pid) = self.pid {
-            let nix_pid = Pid::from_raw(pid as i32);
-            if let Err(e) = signal::kill(nix_pid, Signal::SIGTERM) {
-                tracing::warn!("Failed to send SIGTERM to VM: {}", e);
-            }
+            term_pid(pid);
         }
 
         // Wait for process to exit
@@ -256,10 +289,7 @@
     /// Force kill the VM
     pub async fn kill(&mut self) -> Result<()> {
         if let Some(pid) = self.pid {
-            let nix_pid = Pid::from_raw(pid as i32);
-            if let Err(e) = signal::kill(nix_pid, Signal::SIGKILL) {
-                tracing::warn!("Failed to send SIGKILL to VM: {}", e);
-            }
+            kill_pid(pid);
         }
 
         if let Some(ref mut child) = self.process {
@@ -293,8 +323,7 @@
 
     pub fn is_running(&self) -> bool {
         if let Some(pid) = self.pid {
-            let nix_pid = Pid::from_raw(pid as i32);
-            signal::kill(nix_pid, None).is_ok()
+            pid_alive(pid)
         } else {
             false
         }
@@ -315,8 +344,7 @@
     fn drop(&mut self) {
         // Try to clean up the VM if it's still running
         if let Some(pid) = self.pid {
-            let nix_pid = Pid::from_raw(pid as i32);
-            let _ = signal::kill(nix_pid, Signal::SIGKILL);
+            kill_pid(pid);
         }
         if let Some(ref network) = self.config.network {
             let _ = network.teardown();
--- a/elastos/crates/elastos-guest/src/runtime.rs
+++ b/elastos/crates/elastos-guest/src/runtime.rs
@@ -156,7 +156,7 @@
                 .open(&path)
             {
                 Ok(file) => {
-                    #[cfg(not(target_os = "wasi"))]
+                    #[cfg(all(unix, not(target_os = "wasi")))]
                     {
                         use std::os::fd::AsRawFd;
                         let fd = file.as_raw_fd();
@@ -173,6 +173,13 @@
                         let _ = file;
                         CarrierChannel::Stdio
                     }
+                    // Non-unix hosts (e.g. native Windows): no termios raw-mode, and the
+                    // serial Carrier path is host-unused (only a guest inside a VM sets
+                    // ELASTOS_CARRIER_PATH). Keep the file channel so the type compiles.
+                    #[cfg(all(not(unix), not(target_os = "wasi")))]
+                    {
+                        CarrierChannel::Serial { file }
+                    }
                 }
                 Err(e) => {
                     panic!(
@@ -202,7 +209,7 @@
     /// Configure a serial device fd for raw mode (no echo, no line discipline).
     /// Must be called on a fd that stays open — Linux resets TTY settings
     /// when all fds to the device close.
-    #[cfg(not(target_os = "wasi"))]
+    #[cfg(all(unix, not(target_os = "wasi")))]
     fn configure_serial_raw_fd(fd: i32) -> io::Result<()> {
         unsafe {
             let mut termios: libc::termios = std::mem::zeroed();
@@ -542,7 +549,7 @@
             })
         }
 
-        #[cfg(not(target_os = "wasi"))]
+        #[cfg(all(unix, not(target_os = "wasi")))]
         {
             use std::os::unix::io::FromRawFd;
             let reader = unsafe { std::fs::File::from_raw_fd(read_fd) };
@@ -552,6 +559,16 @@
                 writer,
             })
         }
+        // Non-unix hosts: integer fds aren't File handles, and the fd-pair Carrier
+        // channel is guest-in-VM only, so report unsupported rather than fabricate one.
+        #[cfg(all(not(unix), not(target_os = "wasi")))]
+        {
+            let _ = (read_fd, write_fd);
+            Err(io::Error::new(
+                io::ErrorKind::Unsupported,
+                "fd-based carrier channel is not supported on this platform",
+            ))
+        }
     }
 
     #[cfg(any(test, not(target_os = "wasi")))]
@@ -583,13 +600,15 @@
 
     #[cfg(not(target_os = "wasi"))]
     fn serial_write_line(file: &mut std::fs::File, json: &str) -> io::Result<()> {
-        use std::os::fd::AsRawFd;
-
         file.write_all(json.as_bytes())?;
         file.write_all(b"\n")?;
         file.flush()?;
 
+        // Drain the UART tty so the line is flushed to the wire (unix only;
+        // no tty/termios on non-unix hosts, where this path is host-unused).
+        #[cfg(unix)]
         unsafe {
+            use std::os::fd::AsRawFd;
             let fd = file.as_raw_fd();
             if libc::isatty(fd) == 1 {
                 let _ = libc::tcdrain(fd);
--- a/elastos/crates/elastos-server/src/lib.rs
+++ b/elastos/crates/elastos-server/src/lib.rs
@@ -33,4 +33,8 @@
 pub mod sources;
 pub mod supervisor;
 pub mod update;
+// vm_provider bridges to microVM capsules over a vsock/poll guest control
+// channel — Linux-only. microVMs are unsupported off-unix (crosvm::is_supported()
+// is false), so the module is compiled out on non-unix targets (e.g. Windows).
+#[cfg(unix)]
 pub mod vm_provider;
--- a/elastos/crates/elastos-server/src/carrier_bridge.rs
+++ b/elastos/crates/elastos-server/src/carrier_bridge.rs
@@ -52,6 +52,20 @@
 /// Must be called BEFORE starting the VM so the socket exists when crosvm launches.
 /// Reads `RequestEnvelope` JSON lines, dispatches to providers,
 /// writes `ResponseEnvelope` JSON lines back.
+// microVM Carrier bridge uses a Unix socket (crosvm virtio-console) — Unix-only,
+// dead-in-lite. The WASM stdio bridge in this file (OS pipes) is the path lite
+// uses and stays cross-platform. Stub on non-unix so callers compile.
+#[cfg(not(unix))]
+pub async fn spawn_carrier_bridge(
+    _socket_path: &Path,
+    _provider_registry: Arc<ProviderRegistry>,
+    _session_token: String,
+    _bridge_ctx: Option<BridgeContext>,
+) -> Result<()> {
+    anyhow::bail!("microVM Carrier bridge is unavailable on this platform")
+}
+
+#[cfg(unix)]
 pub async fn spawn_carrier_bridge(
     socket_path: &Path,
     _provider_registry: Arc<ProviderRegistry>,
--- a/elastos/crates/elastos-server/src/host_lock.rs
+++ b/elastos/crates/elastos-server/src/host_lock.rs
@@ -1,5 +1,6 @@
 use std::fs::{self, OpenOptions};
 use std::io::{Read as _, Seek as _, SeekFrom, Write as _};
+#[cfg(unix)]
 use std::os::fd::AsRawFd;
 use std::path::{Path, PathBuf};
 use std::time::Duration;
--- a/elastos/crates/elastos-server/src/room_service.rs
+++ b/elastos/crates/elastos-server/src/room_service.rs
@@ -1,6 +1,7 @@
 use std::collections::{BTreeMap, BTreeSet};
 use std::fs;
 use std::io::{Seek, SeekFrom};
+#[cfg(unix)]
 use std::os::fd::AsRawFd;
 use std::path::{Path, PathBuf};
 use std::time::{SystemTime, UNIX_EPOCH};
--- a/elastos/crates/elastos-server/src/runtime_control.rs
+++ b/elastos/crates/elastos-server/src/runtime_control.rs
@@ -252,8 +252,13 @@
     owner.pid == std::process::id() && matches!(owner.role.as_str(), "gateway" | "gateway-public")
 }
 
+// Interactive raw-mode TTY (libc termios) — Unix-only. The desktop/Windows build
+// never runs the interactive CLI; #[cfg(not(unix))] stubs below keep callers
+// (run_cmd/chat_cmd/home_cmd) compiling.
+#[cfg(unix)]
 struct SavedTermios(libc::termios);
 
+#[cfg(unix)]
 fn enable_host_raw_mode() -> Option<SavedTermios> {
     unsafe {
         let mut original: libc::termios = std::mem::zeroed();
@@ -289,6 +294,7 @@
     }
 }
 
+#[cfg(unix)]
 fn restore_host_terminal(saved: SavedTermios) {
     unsafe {
         libc::tcsetattr(libc::STDIN_FILENO, libc::TCSANOW, &saved.0);
@@ -297,12 +303,15 @@
 
 /// Public wrapper for raw mode — used by run_cmd, chat_cmd, and home_cmd.
 /// Returns a guard that restores the terminal on drop.
+#[cfg(unix)]
 pub fn enable_host_raw_mode_pub() -> Option<TermiosGuard> {
     enable_host_raw_mode().map(|s| TermiosGuard(Some(s)))
 }
 
+#[cfg(unix)]
 pub struct TermiosGuard(Option<SavedTermios>);
 
+#[cfg(unix)]
 impl Drop for TermiosGuard {
     fn drop(&mut self) {
         if let Some(saved) = self.0.take() {
@@ -311,6 +320,16 @@
     }
 }
 
+// Non-unix (Windows): interactive raw-mode TTY is unavailable; stubs keep
+// cross-platform callers compiling.
+#[cfg(not(unix))]
+pub struct TermiosGuard;
+
+#[cfg(not(unix))]
+pub fn enable_host_raw_mode_pub() -> Option<TermiosGuard> {
+    None
+}
+
 pub fn runtime_coord_path(data_dir: &Path) -> PathBuf {
     if let Some(path) = std::env::var_os("ELASTOS_RUNTIME_COORDS_FILE") {
         return PathBuf::from(path);
--- a/elastos/crates/elastos-server/src/home_cmd.rs
+++ b/elastos/crates/elastos-server/src/home_cmd.rs
@@ -1796,6 +1796,7 @@
     "ready".to_string()
 }
 
+#[cfg(unix)]
 fn current_terminal_size() -> Option<(u16, u16)> {
     let mut ws: libc::winsize = unsafe { std::mem::zeroed() };
     let ok = unsafe { libc::ioctl(libc::STDOUT_FILENO, libc::TIOCGWINSZ, &mut ws) };
@@ -1806,6 +1807,13 @@
     }
 }
 
+/// TIOCGWINSZ is a Linux terminal ioctl; off-unix (e.g. the native Windows
+/// desktop build) the interactive home TTY path is unused, so report no size.
+#[cfg(not(unix))]
+fn current_terminal_size() -> Option<(u16, u16)> {
+    None
+}
+
 fn home_debug_tty() -> bool {
     matches!(
         std::env::var("ELASTOS_HOME_DEBUG_TTY").ok().as_deref(),
--- a/elastos/crates/elastos-server/src/supervisor.rs
+++ b/elastos/crates/elastos-server/src/supervisor.rs
@@ -17,6 +17,7 @@
 use crate::carrier_service::CarrierServiceProvider;
 use crate::ownership;
 use crate::setup::{CapsuleEntry, ComponentsManifest};
+#[cfg(unix)]
 use crate::vm_provider::VmCapsuleProvider;
 
 use elastos_crosvm::{CrosvmConfig, NetworkConfig, RunningVm, VmConfig};
@@ -478,6 +479,21 @@
         }
     }
 
+    /// VM-backed provider routes require a microVM control channel (Linux-only).
+    /// Off-unix (e.g. native Windows) microVMs are unsupported, so there is no
+    /// route to register.
+    #[cfg(not(unix))]
+    async fn register_provider_route(
+        &self,
+        _capsule_name: &str,
+        _provides: Option<&str>,
+        _guest_ip: &str,
+        _init_config: serde_json::Value,
+    ) -> Option<ProviderRoute> {
+        None
+    }
+
+    #[cfg(unix)]
     async fn register_provider_route(
         &self,
         capsule_name: &str,
@@ -976,13 +992,18 @@
         // Serial consoles lack TIOCGWINSZ and default to TERM=linux which causes
         // key misinterpretation when the host terminal is xterm-256color etc.
         if interactive_stdio {
-            let mut ws: libc::winsize = unsafe { std::mem::zeroed() };
-            let ok = unsafe { libc::ioctl(libc::STDOUT_FILENO, libc::TIOCGWINSZ, &mut ws) };
-            if ok == 0 && ws.ws_col > 0 && ws.ws_row > 0 {
-                vm_config.boot_args = format!(
-                    "{} elastos.term_cols={} elastos.term_rows={}",
-                    vm_config.boot_args, ws.ws_col, ws.ws_row
-                );
+            // TIOCGWINSZ is a Linux terminal ioctl; microVMs (and thus interactive
+            // serial consoles) are Linux-only, so this is compiled out off-unix.
+            #[cfg(unix)]
+            {
+                let mut ws: libc::winsize = unsafe { std::mem::zeroed() };
+                let ok = unsafe { libc::ioctl(libc::STDOUT_FILENO, libc::TIOCGWINSZ, &mut ws) };
+                if ok == 0 && ws.ws_col > 0 && ws.ws_row > 0 {
+                    vm_config.boot_args = format!(
+                        "{} elastos.term_cols={} elastos.term_rows={}",
+                        vm_config.boot_args, ws.ws_col, ws.ws_row
+                    );
+                }
             }
             // Pass host TERM so guest crossterm generates matching escape sequences.
             if let Ok(term) = std::env::var("TERM") {
