[package]
name = "tunnel-provider"
version = "0.1.0"
edition = "2021"
description = "ElastOS tunnel provider - manages public quick tunnels"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
