[package]
name = "drm-provider"
version = "0.1.0"
edition = "2021"
description = "ElastOS protected content provider capsule"
license = "MIT"

[[bin]]
name = "drm-provider"
path = "src/main.rs"

[dependencies]
elastos-common = { path = "../../elastos/crates/elastos-common" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]

[profile.release]
opt-level = "s"
lto = true

[workspace]
