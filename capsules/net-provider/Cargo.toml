[package]
name = "net-provider"
version = "0.1.0"
edition = "2021"
description = "ElastOS Browser Net provider capsule"
license = "MIT"

[[bin]]
name = "net-provider"
path = "src/main.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
url = "2"

[dev-dependencies]

[profile.release]
opt-level = "s"
lto = true

[workspace]
