[package]
name = "browser-engine-supervisor"
version = "0.1.0"
edition = "2021"
description = "ElastOS Browser Engine host supervisor"
license = "MIT"

[workspace]

[dependencies]
libc = "0.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
