#:schema https://raw.githubusercontent.com/YunoHost/apps/master/schemas/tests.v1.schema.json

test_format = 1.0

[default]
# multi_instance = false in the manifest; the multi-install test cannot pass.
exclude = ["install.multi"]

# Install args come from manifest.toml defaults (domain, path=/, visitors, admin).

# Upgrade from the last shipped 0.7.0~ynh2 (Hyper on the dock, three remaining
# patches). package_check clones this commit, installs it, then upgrades to HEAD.
test_upgrade_from.7e6f643061cf88d791fe0a6f7eace55390997e7d.name = "Upgrade from 0.7.0~ynh2"
